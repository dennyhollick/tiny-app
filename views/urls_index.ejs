<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
</head>
<body>
    <h1>Tiny App URLs</h1>

   <ul>
    <% for (url in urls) { %>
       <li> <%= url + " - " + urls[url] %> <form method="POST" action="/urls/<%= url %>/delete"> <input type="submit" value="Delete"> </form> </li>
    <% } %>

   </ul>

</body>
</html>