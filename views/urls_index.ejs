<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
</head>
<body>
    <h1>Tiny App URLs</h1>

   <ul>
    <% for (url in urls) { %>
       <li> <%= url + " - " + urls[url] %></li>
    <% } %>

   </ul>

</body>
</html>